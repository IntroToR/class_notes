---
title: "johana_coronel"
output: html_document
---

```{r}
library(tidyverse)
install.packages('dplyr')
library(dplyr)
mtcars_arrange = mtcars
mtcars
```

Question 1
```{r}
mtcars_arrange = arrange(mtcars, cyl, gear)
mtcars_arrange
```

Question 2
```{r}
model= mutate(mtcars_arrange, rownames(mtcars_arrange))
model
```

Question 3 
```{r}
model= (gsub("", "_", model))
model
```


Question 4
```{r}
mpg_low = filter(mtcars_arrange, mpg<20)
mpg_low

mpg_high = filter(mtcars_arrange, mpg >= 20)
mpg_high
```
```

```{r}
mtcars_arrange %>%
  arrange(hp)%>%
  select (cyl, hp, model, gear)%>%
  mutate(new_variable = cyl * gear)